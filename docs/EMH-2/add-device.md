---
title: Add Device
description: Add an EMH-2 device in the Energy Ease App and view device status and connected sub-devices.
---

# Add Device

After adding the EMH-2 device in the Energy Ease App, you can view device operating data, network status, and information about connected sub-devices.

:::tip
- Make sure the device is powered on and has started normally.
- The QR code can be found on the label at the bottom of the device.
:::

1. Tap **"+"** in the upper-right corner of the App home page, then select **"Add Device"**.
2. Tap the <img src={require("./img/scan_icon.png").default} width="25" style={{verticalAlign: "middle"}}/> icon in the upper-right corner and scan the QR code on the device.
3. Enter the network configuration page. Currently, only local Web-based network configuration is supported. Refer to [Gateway Settings](./device.md#3-gateway-settings). Tap **"Skip"** to skip this step.
4. After the device is added successfully, you can view the following information on the device details page:
   - Network connection information
   - Connected sub-device list
   - Basic device configuration and management options

<img src={require("./img/tap_add_device.png").default} width="240" /> 
<img src={require("./img/add_device.png").default} width="240" /> 
<img src={require("./img/scan_qr_code.png").default} width="240" /> 
<img src={require("./img/network_config.png").default} width="240" /> 
<img src={require("./img/device_info.png").default} width="240" /> 
